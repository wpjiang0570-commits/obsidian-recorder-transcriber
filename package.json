{
    "name": "obsidian-recorder-transcriber",
    "version": "1.0.0",
    "description": "Obsidian plugin to transcribe audio recordings to notes",
    "main": "main.js",
    "scripts": {
        "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs",
        "dev": "node esbuild.config.mjs --watch"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "builtin-modules": "^3.3.0",
        "esbuild": "^0.19.0",
        "obsidian": "^1.5.0",
        "tslib": "^2.6.0",
        "typescript": "^5.3.0"
    }
}
